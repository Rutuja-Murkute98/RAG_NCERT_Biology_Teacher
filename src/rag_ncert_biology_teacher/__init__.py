"""RAG-based NCERT Class 12 Biology teacher chatbot.

The real entry point is the Flask app in app/server.py (run with
`uv run python app/server.py`, or `gunicorn app.server:app` in production) -
see README.md for the full setup and pipeline walkthrough.
"""
